var users = ["bilal", "hira", "faizan", "admin"];
if (users.length === 0) {
    console.log(" We need to find some user! ");
}
//rempve all usser name from array
else {
    users = [];
    console.log("all user have been removed   " + users.length);
}
